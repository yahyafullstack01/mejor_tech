import styles from "../About_us/about.module.css";
export default function About() {
    
    return (
        <div className={`${styles.container} ${"items-center justify-center bg-slate-400 h-screen"}`}>
            hello every one
        </div>
    )
}